# Đầu ra cục bộ

Checkpoint, model export, log lớn và ảnh kết quả để tại đây; nội dung được `.gitignore` bỏ qua. Run tương ứng cần có hồ sơ trong `../docs/03-baseline/` hoặc `../docs/05-evaluation/`, cùng mục bàn giao ở `../docs/00-project/external-assets.md` nếu người kia cần file.
